@model IEnumerable<GTERP.Models.ReportPermissions>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserId)
        </th>
        <th>

        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ReportPermissionsId }) |
                @Html.ActionLink("Details", "Details", new { id = item.ReportPermissionsId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ReportPermissionsId })
            </td>
        </tr>
    }

</table>
